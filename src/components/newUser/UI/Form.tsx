import styled from "styled-components";

const Form = styled.div`
  display: flex;
  overflow: hidden;
  margin-top: 2rem;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.26);
  border-radius: 10px;
`;

export default Form;
